# Tests

- **login/** — TC001–TC003
- **cart/** — TC004–TC006
- **pages/** — BasePage, LoginPage, ProductsPage, CartPage
- **fixtures/** — `pages.fixture.ts` (injects page objects)
- **helpers/** — `testData.helper.ts`

Run: `npm test` or `npx playwright test tests/login` (or `tests/cart`).
